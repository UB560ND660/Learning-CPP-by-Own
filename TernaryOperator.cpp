#include <iostream>

int main() {
    
    int grade = 75;

    // (grade >=70) ? std::cout << "You are pass" : std::cout << "You are failed"; 

    // int number = 8;

    // number % 2 == 1 ? std::cout << "Odd" : std::cout << "Even";

    bool hungry = false;

    // hungry ? std::cout << "Yes!" : std::cout << "No!";

    std::cout << (hungry ? "Yes!" :  "No!");

    return 0;
}